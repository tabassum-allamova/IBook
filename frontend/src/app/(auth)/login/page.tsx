import { LoginForm } from '@/components/auth/LoginForm'

export const metadata = {
  title: 'Sign In | IBook',
  description: 'Sign in to your IBook account',
}

export default function LoginPage() {
  return <LoginForm />
}
